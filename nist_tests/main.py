import tests as nist
from random import randint
from tqdm import tqdm

n = 40000
a = [0 for i in range(n)]

# nist.Frequency(a)
# nist.BlockFrequency(a, 20000)
# nist.Runs(a)
# nist.LongestRunOfOnes_128(a)
# nist.LongestRunOfOnes_6272(a)
# nist.LongestRunOfOnes_750000(a)